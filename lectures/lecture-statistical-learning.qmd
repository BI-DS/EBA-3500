# Lecture 9 {.unnumbered}

1. Concepts of statistical learning
  * 
2. Loading and exploring data.
  * `read_csv` (index_col and na argument).
  * `frame.info()`.
  * `frame.head()`.
  * `frame.drop_na()`.
  * `frame.corr()`.
  * Use `sns.pairplot(frame)` to plot the pairwise correlations between all numeric variables in a data frame.
  * Removing and replacing data.